## 书信息
demo:
  book:
    name: spring boot
    writer: writer

spring:
  jpa:
    ## 是否显示 SQL 语句
    show-sql: true
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: 127.0.0.1
    username: root
    password:

# springboot默认static中放静态页面，而templates中放动态页面

example:
  service:
    enable: true
    prefix: spring
    suffix: boot
